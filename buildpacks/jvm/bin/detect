#!/usr/bin/env bash

# shellcheck disable=SC2034
app_dir="$(pwd)"
# shellcheck disable=SC2034
platform_dir="${1:?}"
# shellcheck disable=SC2034
build_plan="${2:?}"

cat <<-EOF >"${build_plan}"
	[[provides]]
	name = "jdk"
EOF

exit 0
